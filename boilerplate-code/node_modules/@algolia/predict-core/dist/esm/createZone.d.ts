import { Zone } from './predict';
export declare const zoneCounterRef: {
    current: number;
};
export declare type ZoneOptions = {
    id?: Zone['id'];
    name: Zone['name'];
    active?: Zone['active'];
    happened?: Zone['happened'];
};
export declare function createZone({ id, name, active, happened, }: ZoneOptions): Zone;
