#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -z "$NODE_PATH" ]; then
  export NODE_PATH="/Users/hugo.valla/Desktop/DEMO 🏅/algolia-react-boilerplate/node_modules/.pnpm/node_modules"
else
  export NODE_PATH="$NODE_PATH:/Users/hugo.valla/Desktop/DEMO 🏅/algolia-react-boilerplate/node_modules/.pnpm/node_modules"
fi
if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../node_modules/.pnpm/@commitlint+cli@17.3.0/node_modules/@commitlint/cli/cli.js" "$@"
else
  exec node  "$basedir/../../../../node_modules/.pnpm/@commitlint+cli@17.3.0/node_modules/@commitlint/cli/cli.js" "$@"
fi
