import { type Zone } from '@algolia/predict-core';
import { type ComponentProps, type ReactNode } from 'react';
import { type PredictApi } from './usePredict';
export declare type PredictZoneProps = Omit<ComponentProps<'div'>, 'onMouseDown' | 'onTouchStart'> & {
    id?: Zone['id'];
    name: Zone['name'];
    segment?: string | ((api: PredictApi) => boolean);
    children: ReactNode;
    fallback?: ReactNode;
};
export declare function PredictZone({ id, children, name, segment, fallback, ...props }: PredictZoneProps): JSX.Element;
