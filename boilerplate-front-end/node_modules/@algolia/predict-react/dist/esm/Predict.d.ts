import { type UserProfile } from '@algolia/predict-core';
import { ReactNode } from 'react';
export declare type PredictProps = {
    userProfile: UserProfile;
    suppressExperimentalWarning?: boolean;
    children: ReactNode;
};
export declare function Predict({ userProfile, suppressExperimentalWarning, children, }: PredictProps): JSX.Element;
