import { type UserProfile } from '@algolia/predict';
export declare type PersonifierParams = {
    onPersonificationStart: (params: {
        userProfile: UserProfile;
    }) => void;
    onPersonificationEnd: () => void;
};
export declare function createPersonifier({ onPersonificationStart, onPersonificationEnd, }: PersonifierParams): {
    isPersonifying(): boolean;
    start({ userProfile }: {
        userProfile: UserProfile;
    }): void;
    end(): void;
    loadUserProfileFromStorage(): void;
};
