import { type Predictions } from '@algolia/predict';
/**
 * Unwraps the prediction value, or falls back when it's unavailable.
 *
 * If the model is unavailable, it returns the fallback and triggers a warning.
 */
export declare function unwrapPrediction<TType>(predictions: Predictions | undefined, model: keyof Predictions, fallback: TType): TType;
